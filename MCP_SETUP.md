# MCP Setup - Model Context Protocol

## Что такое MCP?

MCP (Model Context Protocol) позволяет AI боту использовать внешние инструменты - файловую систему, GitHub, базы данных и другие сервисы.

## Конфигурация

### Файл mcp.json

**Все MCP серверы настраиваются в файле `mcp.json`** (не в `.env`)

```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/app/mcp_workspace"
      ],
      "enabled": true,
      "description": "Работа с файлами и папками"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {"GITHUB_TOKEN": "ghp_your_token_here"},
      "enabled": false,
      "description": "Управление GitHub репозиториями"
    }
  }
}
```

### Переменные окружения (.env)

В `.env` больше нет MCP переменных - все настройки в `mcp.json`:

```bash
# Telegram Bot
TG_BOT_TOKEN=your_token
# ... другие настройки
# MCP переменные удалены - используйте mcp.json
```

## Быстрый старт

### 1. Создать mcp.json

```bash
cp mcp.json.example mcp.json
```

### 2. Настроить путь к workspace

**Важно:** Укажите правильный путь в зависимости от способа запуска:

| Среда | Путь в mcp.json |
|-------|-----------------|
| Docker | `/app/mcp_workspace` |
| Локально | `./mcp_workspace` или полный путь |

Пример для Docker:
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/app/mcp_workspace"],
      "enabled": true
    }
  }
}
```

Пример для локального запуска:
```json
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/user/dev/aichatbot/mcp_workspace"],
      "enabled": true
    }
  }
}
```

### 3. Включить/выключить инструменты

По умолчанию MCP инструменты включены для всех пользователей. Вы можете отключить их для себя:

```
/mcp off    # отключить MCP инструменты
/mcp on     # включить MCP инструменты
/mcp        # показать текущий статус
```

## Команды бота для MCP

| Команда | Описание | Кому доступно |
|---------|----------|---------------|
| `/tools` | Список доступных MCP инструментов | Все авторизованные |
| `/mcp on` | Включить MCP инструменты | Все авторизованные |
| `/mcp off` | Выключить MCP инструменты | Все авторизованные |
| `/mcp` | Показать текущий статус MCP | Все авторизованные |
| `/mcpstatus` | Статус MCP серверов | Только администратор |

## Пример использования

```
Вы: Какие файлы есть в workspace?

Bot: [Вызывает инструмент list_directory через MCP]

🔧 Available MCP Tools:

📦 filesystem (14 tools)
  - read_file
  - write_file
  - list_directory
  ...

В рабочем каталоге /app/mcp_workspace:
- README.md
- todo_cli.py
- touch.me
```

## Доступные серверы

В `mcp.json.example` есть примеры для:

| Сервер | Описание | Требует токен |
|--------|----------|---------------|
| **filesystem** | Работа с файлами и папками | Нет |
| **github** | Репозитории, issues, PR | GITHUB_TOKEN |
| **postgres** | PostgreSQL база данных | Нет (connection string) |
| **sqlite** | SQLite база данных | Нет |
| **brave-search** | Поиск в интернете | BRAVE_API_KEY |
| **puppeteer** | Автоматизация браузера | Нет |
| **slack** | Интеграция со Slack | SLACK_BOT_TOKEN |
| **google-maps** | Карты и маршруты | GOOGLE_MAPS_API_KEY |

## Добавление нового сервера

1. Открыть `mcp.json`
2. Добавить секцию сервера
3. Установить `enabled: true`
4. Перезапустить бота

Пример добавления GitHub:
```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {"GITHUB_TOKEN": "ghp_1234567890"},
      "enabled": true,
      "description": "GitHub integration"
    }
  }
}
```

## Структура проекта

```
aichatbot/
├── mcp.json              # Конфигурация MCP серверов (создаётся пользователем)
├── mcp.json.example      # Пример конфигурации
├── mcp_workspace/        # Рабочая директория для filesystem (gitignored)
├── bot.py                # Основной код бота
├── mcp_manager.py        # MCP менеджер
└── MCP_SETUP.md          # Этот файл
```

## Troubleshooting

| Проблема | Решение |
|----------|---------|
| MCP не работает | Проверьте валидность `mcp.json`: `python -c "import json; json.load(open('mcp.json'))"` |
| Нет инструментов | `/mcpstatus` — проверить статус серверов |
| Ошибка подключения | Проверьте `enabled: true` в mcp.json |
| Путь не найден | Укажите полный путь к workspace для локального запуска |
| Нет npx в Docker | npx уже включён в Docker образ |

## Проверка работоспособности

```bash
# 1. Проверить JSON валидность
python -c "import json; json.load(open('mcp.json'))"

# 2. Запустить бота
docker-compose up -d bot

# 3. В логах должно быть:
# "Loaded filesystem server: command=npx..."
# "MCPServerManager initialized with X server configs"

# 4. В боте:
# /tools - показать инструменты
# /mcpstatus - проверить статус (админ)
```

## Как AI использует инструменты?

Когда вы отправляете сообщение, бот:
1. Проверяет, включены ли MCP инструменты (`/mcp`)
2. Загружает список доступных инструментов из `mcp.json`
3. Передаёт их в API как `tools` параметр
4. AI решает, какие инструменты использовать
5. Если AI вызывает инструмент — бот выполняет его через MCP
6. Результат отправляется обратно в API для финального ответа

**Пример диалога:**
```
Вы: Прочитай файл README.md в workspace
Bot: [read_file → "Содержимое README.md..."]
     Файл README.md содержит следующее:
     Содержимое README.md...
```
