services:
  bot:
    build: .
    container_name: telegram-bot-async
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./mcp_workspace:/app/mcp_workspace
      - ./mcp.json:/app/mcp.json:ro
    depends_on:
      minio:
        condition: service_healthy
      minio-setup:
        condition: service_completed_successfully
    environment:
      - MINIO_ENDPOINT=http://minio:9000
      # AsyncTeleBot environment
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "python", "-c", "import asyncio; import sys; sys.exit(0)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    # Resource limits для async bot (может обрабатывать больше запросов)
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M

  minio:
    image: minio/minio:latest
    container_name: minio
    ports:
      - "${MINIO_API_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M

  minio-setup:
    image: minio/mc:latest
    container_name: minio-setup
    depends_on:
      minio:
        condition: service_healthy
    environment:
      - MINIO_ROOT_USER=${MINIO_ROOT_USER}
      - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
      - S3_BUCKET=${S3_BUCKET}
      - S3_KEY_ID=${S3_KEY_ID}
      - S3_KEY_SECRET=${S3_KEY_SECRET}
    entrypoint: >
      /bin/sh -c "
      sleep 3 &&
      /usr/bin/mc alias set myminio http://minio:9000 $$MINIO_ROOT_USER $$MINIO_ROOT_PASSWORD &&
      /usr/bin/mc mb myminio/$$S3_BUCKET --ignore-existing &&
      /usr/bin/mc admin user add myminio $$S3_KEY_ID $$S3_KEY_SECRET &&
      /usr/bin/mc admin policy attach myminio readwrite --user $$S3_KEY_ID &&
      echo '=== MinIO Setup Complete ===' &&
      echo 'Bucket created: $$S3_BUCKET' &&
      echo 'User created: $$S3_KEY_ID'
      "
    restart: "no"

volumes:
  minio_data:
    driver: local
