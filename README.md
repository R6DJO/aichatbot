# Telegram AI Chat Bot

Telegram бот с multi-modal AI возможностями: текстовые чаты, анализ изображений, генерация картинок, голосовые сообщения (транскрибация + TTS).

## Возможности

- 📝 **Текстовые чаты** — поддержка различных AI моделей (GLM, GPT, Qwen, Codex)
- 🖼️ **Анализ изображений** — GPT-4 Vision для анализа картинок
- 🎨 **Генерация изображений** — DALL-E 3
- 🎤 **Голосовые сообщения** — Whisper транскрибация + TTS ответ
- 💾 **История чата** — персистентность в S3-совместимом хранилище
- 🔐 **Контроль доступа** — whitelist по username
- 🤖 **Переключение моделей** — выбор AI модели для текстовых чатов

## Требования

- Python 3.10+
- Docker и Docker Compose (для MinIO)
- Telegram Bot Token
- OpenAI API Key или совместимый API

## Установка

### 1. Клонирование и настройка окружения

```bash
# Создаем виртуальное окружение
python -m venv venv
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```bash
# Telegram Bot
TG_BOT_TOKEN=your_telegram_bot_token
TG_BOT_CHATS=your_username,another_username

# OpenAI API
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=http://localhost:8317/v1

# MinIO (для S3 хранилища)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
S3_KEY_ID=botuser
S3_KEY_SECRET=botpassword123
S3_BUCKET=aichatbot
MINIO_ENDPOINT=http://localhost:9000
```

### 3. Запуск MinIO

```bash
docker-compose up -d
```

При первом запуске автоматически создаются:
- Бакет `aichatbot`
- Пользователь `botuser` с доступом readwrite

Проверьте создание:
```bash
docker logs minio-setup
```

### 4. Запуск бота

```bash
python bot.py
```

## Команды бота

| Команда | Описание |
|---------|----------|
| `/start` или `/help` | Приветственное сообщение |
| `/models` | Список доступных AI моделей (группировка по производителю) |
| `/model <название>` | Выбор модели для текстовых чатов |
| `/new` | Очистка истории чата |
| `/image <запрос>` | Генерация изображения через DALL-E 3 |

### Примеры

```
/models
📋 Доступные модели:

🏢 openai
  gpt-5
  gpt-5.1
  ▶️ gpt-5.2
  ...

🏢 z.ai
  glm-4.7
  ...

/model gpt-5.2
✅ Модель изменена на: gpt-5.2

/image кошка в космосе
[сгенерирует изображение]

/new
История чата очищена!
```

## Архитектура

```
Telegram → bot.py → OpenAI API
                ↓
            Yandex S3 / MinIO
        (история чата + настройки)
```

- **bot.py** — основной файл бота (~370 строк)
- **Pooling режим** — бот постоянно опрашивает Telegram API
- **S3 хранилище** — история чата `{chat_id}.json`, настройки `{chat_id}_settings.json`

## S3-совместимые хранилища

Бот работает с любыми S3-совместимыми хранилищами:

| Хранилище | Пример MINIO_ENDPOINT |
|-----------|----------------------|
| MinIO | `http://localhost:9000` |
| Yandex Cloud | (не указывать, используется по умолчанию) |
| AWS S3 | `https://s3.amazonaws.com` |

Подробнее в [MINIO_SETUP.md](MINIO_SETUP.md).

## Деплой на AWS Lambda

Вебхук-режим поддерживается функцией `handler()` для деплоя на AWS Lambda.

## Структура файлов

```
aichatbot/
├── bot.py              # Основной код бота
├── requirements.txt    # Python зависимости
├── docker-compose.yml  # MinIO конфигурация
├── .env.example        # Пример переменных окружения
├── .gitignore
├── README.md
└── MINIO_SETUP.md      # Подробная документация по MinIO
```

## Troubleshooting

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверьте `TG_BOT_CHATS` — ваш username должен быть в списке |
| Ошибка S3 подключения | Проверьте `MINIO_ENDPOINT` и учётные данные |
| Модели не загружаются | Проверьте `OPENAI_BASE_URL/models` endpoint |
| Docker контейнер не стартует | `docker logs minio` и `docker logs minio-setup` |
